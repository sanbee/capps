#include <iostream.h>
#include <vector.h>
#include <string>
#include <mp.h>
#include <fstream>

main()
{
  vector<char *> List;
  ifstream infile;

  //  infile.open("xxxx");

  //  if (!infile) cerr << "error" << endl;
    MakeColorList(List);
}
