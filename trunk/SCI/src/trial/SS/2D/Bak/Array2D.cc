#include <stdlib.h>
#include <Array2D.h>

/*
template <class T> Array2D<T>::Array2D<T>(int N,int M)
{
  buf = (T *)malloc(sizeof(T)*N*M);
  Ny=N; Nx=M;
}

template <class T> Array2D<T>::~Array2D<T>()
{
  if (buf) free(buf);
  buf = NULL;
}

template <class T> void Array2D<T>::resize(int N, int M)
{
  buf = (T *)realloc(buf,sizeof(T)*N*M);
  Ny=N;Nx=M;
}
*/
